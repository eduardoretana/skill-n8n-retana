---
trigger: always_on
description: Documentation and cross-agent alignment for the n8n skill repo
---

# Documentation Rules

- Keep `AGENTS.md`, `CLAUDE.md`, `GEMINI.md`, and `adapters/` aligned with the Codex skill.
- Document setup or behavior changes in `README.md`.
- Never write secrets into this repo.
